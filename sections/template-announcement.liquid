<div class="section-announcement">
  <div class="container-announcement">
    <span>test</span>
    <span>Limited Time Free Shipping on all US Orders!</span>
    <span>USD</span>
  </div>
</div>

{% schema %}
{
  "name": "t:sections.announcement-bar.name",
  "settings": [
    {
        "type": "checkbox",
        "id": "show_country_selector",
        "label": "t:sections.footer.settings.country_selector_show.label",
        "default": true
      },
      {
        "type": "checkbox",
        "id": "show_locale_selector",
        "label": "t:sections.footer.settings.language_selector_show.label",
        "default": true
      }
    ]
}
{% endschema %}